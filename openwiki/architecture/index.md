# Files

- [OpenRouter client (requests, retries, sources, usage)](openrouter-client.md) - How src/openrouter.ts wraps OpenRouter chat completions — request body and server-tool construction, the 3-attempt retry loop with backoff and Retry-After, status-to-error mapping, source extraction from citations and annotations, and usage/cost aggregation in src/cost.ts.
- [Result model & output formatting](output-and-formatting.md) - The PipelineResult contract in src/types.ts and how src/formatters.ts and src/cli.ts render it — decision brief / report / raw / retrieve markdown, the --json payload keys, the cost footer, the X report appendix, stderr channels, and parseDecisionAnswer tolerances in src/modes.ts.
- [System overview & runtime domains](overview.md) - High-level map of grok-cli — the src/cli.ts entrypoint, args/config resolution, runMode pipeline dispatch, the single OpenRouter client, formatter-to-stdout dispatch with the stderr diagnostic channel and exit-code contract, plus the module table and where each runtime domain's wiki pages live.
- [Pipeline orchestration (runMode)](pipeline.md) - How src/modes.ts drives every research run — dispatch order from schema to multi to retrieve to deepresearch to a single Grok call, the withBudget --max-cost guard and MaxCostExceededError, the two-pass --json+web and --schema+web invariant, x/bookmark attachment and injection, and result normalization with parseDecisionAnswer.
